"""Pavlab architecture-figure primitives.

Public surface:
    from pavlab_arch.style import apply_rcparams
    from pavlab_arch.palette import (
        ACCENT, ACCENT_2, ACCENT_3, ACCENT_4, ACCENT_5,
        DET, GRID, TEXT, SUBTLE, SOFT_BG,
        BAR_FACTOR, BAR_TAG, CURATOR_OVERLAY,
        tint,
    )
    from pavlab_arch.primitives import (
        box, arrow, stage_box, perf_gauge, ensemble_proposer,
        fit_text,
    )
    from pavlab_arch.layout import (
        figure, FIGSIZES, grid_columns,
    )
"""
